name: knue_moa
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.10.4

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  http: ^1.2.0
  html: ^0.15.4
  url_launcher: ^6.2.0
  shared_preferences: ^2.2.2
  intl: ^0.19.0
  # 버전 갈등 해결을 위해 0.257.0 버전을 사용합니다.
  lucide_icons: ^0.257.0
  riverpod: ^2.5.1
  flutter_riverpod: ^2.5.1
  hive: ^2.2.3
  hive_flutter: ^1.1.0
  flutter_staggered_animations: ^1.1.0  # 부드러운 애니메이션

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  build_runner: ^2.4.9
  hive_generator: ^2.0.1

flutter:
  # 이 블록은 파일 최하단에 단 한 번만 존재해야 합니다.
  uses-material-design: true